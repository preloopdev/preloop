name: matrix-test
on: push
jobs:
  matrix-job:
    runs-on: self-hosted
    strategy:
      matrix:
        version: [1, 2, 3]
        os: [linux, mac]
      fail-fast: false
    steps:
      - name: Show matrix
        run: echo "version=${{ matrix.version }} os=${{ matrix.os }}"
