name: 105-concurrency-cancellation-group
on: workflow_dispatch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  long-running:
    runs-on: self-hosted
    steps:
      - name: Long running step
        run: sleep 60
