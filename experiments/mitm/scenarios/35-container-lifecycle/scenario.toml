description = "Container lifecycle: job.container context, continue-on-error, conditional steps, pip install."
duration_seconds_max = 600

[[steps]]
kind = "submit_workflow"
path = "35-container-lifecycle.yml"

[[steps]]
kind = "wait_for_event"
event = "job_assigned"
timeout = 120

[[steps]]
kind = "wait_for_event"
event = "job_completed"
timeout = 300
