[workspace]
members = ["cargo:crates/preloop-cli"]

[dist]
allow-dirty = ["ci"]
cargo-dist-version = "0.32.0"
dist = false
packages = ["preloop-cli"]
targets = [
  "aarch64-apple-darwin",
  "x86_64-apple-darwin",
  "aarch64-unknown-linux-gnu",
  "x86_64-unknown-linux-gnu",
]
installers = ["shell", "powershell"]
install-updater = true
checksum = "sha256"
unix-archive = ".tar.gz"
windows-archive = ".zip"
ci = "github"
