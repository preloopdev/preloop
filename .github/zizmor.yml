# Configuration for zizmor static analysis tool
# https://docs.zizmor.sh/configuration/

rules:
  # release.yml is autogenerated by cargo-dist (axodotdev/cargo-dist)
  template-injection:
    ignore:
      - release.yml
  excessive-permissions:
    ignore:
      - release.yml
  unpinned-images:
    ignore:
      - release.yml
